<p style="color: green"><%= notice %></p>

<h1>Posts</h1>
<%= link_to "New post", new_post_path, class:"btn btn-primary mb-4" %>

<div id="posts" class="container-fluid bg-secondary">
  <div class="container p-4">
    <div class="card p-3 mb-4">
        <%= render @mainpost %>
        <p>
          <%= link_to "Show this post", @mainpost, class: "btn btn-secondary" %>
        </p>
    </div>
  </div>
</div>

<div id="posts">
  <% @posts.each do |post| %>
    <div class="card p-3 mb-4">
      <%= render post %>
      <p>
        <%= link_to "Show this post", post, class: "btn btn-secondary" %>
      </p>
    </div>
  <% end %>
</div>

Posts não ativos [<strong><%= @others %></strong>]
